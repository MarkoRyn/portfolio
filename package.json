{
  "name": "portfolio",
  "version": "0.1.0",
  "homepage": "https://markoryn.github.io/portfolio",
  "private": true,
  "devDependencies": {
    "gh-pages": "^4.0.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
